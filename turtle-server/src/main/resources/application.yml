cn:
  xilio:
    turtle:
      upload:
        path: /Users/liuxin/Desktop/turtle/tmp #文件上传根路径
        allowed-extensions: [ jpg, png, pdf,ico ]
      api:
        prefix: /api
        default-page-size: 20
server:
  port: 8000
spring:
  #  sql:
  #    init:
  #      mode: always
  #  r2dbc:
  #    url: r2dbc:h2:mem:///testdb;MODE=MySQL;DB_CLOSE_DELAY=-1
  #    username: sa
  #    password: ''
  r2dbc:
    url: r2dbc:mysql://192.168.0.151:3306/xilio?useSSL=false&serverTimezone=UTC
    username: root
    password: root153212
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  # 文件上传
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      enabled: false # 关闭 multipart 支持
  webflux:
    format:
      date-time: yyyy/MM/dd HH:mm:ss
      date: yyyy/MM/dd
#缓存配置
  cache:
    caffeine:
      spec: maximumSize=500,expireAfterWrite=600s
    type: caffeine

sa-token:
  # token 名称（同时也是 cookie 名称）
  token-name: Authorization
  # token 有效期（单位：秒） 默认30天，-1 代表永久有效
  timeout: 600
  # token 最低活跃频率（单位：秒），如果 token 超过此时间没有访问系统就会被冻结，默认-1 代表不限制，永不冻结
  active-timeout: -1
  # 是否允许同一账号多地同时登录 （为 true 时允许一起登录, 为 false 时新登录挤掉旧登录）
  is-concurrent: true
  # 在多人登录同一账号时，是否共用一个 token （为 true 时所有登录共用一个 token, 为 false 时每次登录新建一个 token）
  is-share: false
  # token 风格（默认可取值：uuid、simple-uuid、random-32、random-64、random-128、tik）
  token-style: uuid
  # 是否输出操作日志
  is-log: true
  token-prefix: Bearer
logging:
  level:
    #    root: debug
    #    org.springframework: debug
    cn.xilio.turtle: debug
