export default defineAppConfig({
   ssr:true,
  ui: {
    colors: {

    },
    button: {
      defaultVariants: {
        // Set default button color to neutral
        // color: 'neutral'
      }
    }
  }
})
